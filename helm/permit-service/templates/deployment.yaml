
apiVersion: apps/v1
kind: Deployment
metadata:
  name: permit-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: permit-service
  template:
    metadata:
      labels:
        app: permit-service
    spec:
      containers:
        - name: permit-service
          image: ghcr.io/agilesolutions/demo:latest
          ports:
            - containerPort: 8080
