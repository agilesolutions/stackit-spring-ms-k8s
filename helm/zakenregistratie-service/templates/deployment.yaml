apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "zakenregistratie-service.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: zakenregistratie-service
  template:
    metadata:
      labels:
        app: zakenregistratie-service
    spec:
      serviceAccountName: zakenregistratie-service
      containers:
        - name: zakenregistratie-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: {{ include "zakenregistratie-service.fullname" . }}
            - secretRef:
                name: {{ include "zakenregistratie-service.fullname" . }}
          resources:
  {{ toYaml .Values.resources | indent 12 }}

livenessProbe:
  httpGet:
    path: /actuator/health/liveness
    port: 8080

readinessProbe:
  httpGet:
    path: /actuator/health/readiness
    port: 8080