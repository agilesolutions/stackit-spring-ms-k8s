apiVersion: v1
kind: Service
metadata:
  name: zakenregistratie-service
spec:
  selector:
    app: zakenregistratie-service
  ports:
    - port: 8080
      targetPort: 8080
  type: {{ .Values.service.type }}