apiVersion: cert-manager.io/v1

kind: Certificate
metadata:
  name: {{ include "vergunning-service.fullname" . }}
  namespace: applications
spec:
  secretName: vergunning-service-tls
  dnsNames:
    - vergunning.platform.example.nl
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer