apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: zakenregistratie-service
  namespace: services
spec:
  interval: 1m
  install:
    createNamespace: true
  chart:
    spec:
      chart: ./helm/zakenregistratie-service
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  values:
    image:
      repository: agilesolutions/zakenregistratie-service
      tag: 1.0.0 # {"$imagepolicy": "flux-system:zakenregistratie-service-policy:tag"}
    service:
      port: 8081
    ingress:
      enabled: true
      className: nginx
      host: zakenregistratie.local
