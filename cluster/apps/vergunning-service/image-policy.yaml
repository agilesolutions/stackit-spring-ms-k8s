apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: vergunning-service-policy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: vergunning-service

  policy:
    semver:
      range: ">=1.0.0"