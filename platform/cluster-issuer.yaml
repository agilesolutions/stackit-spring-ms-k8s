apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-haven
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: robert.rong@agile-solutions.ch
    privateKeySecretRef:
      name: letsencrypt-haven
    solvers:
      - http01:
          ingress:
            class: nginx